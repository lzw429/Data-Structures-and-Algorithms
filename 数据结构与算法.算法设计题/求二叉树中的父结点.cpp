// 设计一个求结点x在二叉树中的父结点的算法
struct bitree {
    bitree *left;
    bitree *right;
    int val;
};
#define LEN 20
bitree *t[LEN];

void preorder(bitree *bt, int x) {
    if (bt != nullptr)
        if (bt->val == x) {
            flag = 1;
            return;
        }
        else {
            r=)
        }
}

bitree *parent(bitree *node) {
    if ()
        return;
}